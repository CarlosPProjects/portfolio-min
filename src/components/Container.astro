---
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<section class={cn("max-w-[887px] mx-auto px-4 sm:px-8 lg:px-0", className)}>
  <slot />
</section>
