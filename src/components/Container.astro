---
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<section class={cn("max-w-[887px] mx-auto px-4 sm:px-8 lg:px-0 py-10 sm:py-12 md:py-16", className)}>
  <slot />
</section>
