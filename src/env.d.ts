/// <reference path="../.astro/types.d.ts" />

interface ISocial {
  name: string;
  icon: string;
  url: string;
}

interface INav {
  name: string;
  url: string;
  featured: boolean;
}

interface IProjects {
  title: string;
  subtitle: string;
  description: string;
  image: string;
  url: string;
}